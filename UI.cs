using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public enum XAlign { Left, Center, Right }
public enum YAlign { Top, Center, Bottom }
public enum Dimension { x = 0, y = 1, z = 2 }
